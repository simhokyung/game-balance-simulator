# ⚔️ 우테코 자유과제 - Game Balance Simulator
직접 설계한 전투 규칙을 바탕으로, 여러 캐릭터 빌드를 수천 번 자동으로 전투시키며 승률·평균 턴 수·남은 체력 등의 지표로 **밸런스를 분석하는 시뮬레이터**를 구현한다.  
작은 단위의 책임으로 도메인을 분리하고 TDD로 검증하면서, “어떤 상태를 밸런스가 잘 잡힌 상태라고 볼 것인지”를 스스로 정의하고 구현하는 것이 목표이다.
> 하드 코딩 금지, 상수 관리, 구현 순서 컨벤션, 단일 책임, indent ≤ 2, `else/switch/3항` 금지 원칙을 따른다.
---

## 📥 입출력 명세
본 프로젝트는 **콘솔** 기반으로 시작하고, 필요 시 이후에 REST API(Sprint Boot)로 확장할 수 있도록 설계한다.

### ▫ 입력
1) 캐릭터 정의 입력
- 캐릭터 수 `N`
- 각 캐릭터의 기본 스탯
  - 이름, HP, 공격력, 방어력, 속도, 치명타 확률
- 예시:
```plaintext
시뮬레이션에 사용할 캐릭터 수를 입력하세요.
3

캐릭터 이름, HP, ATK, DEF, SPD, CRIT(0~1)을 입력하세요.
Warrior,1500,80,50,30,0.1
Mage,900,120,20,40,0.25
Archer,1000,90,30,35,0.15
```
2) 스킬 정의 입력
- 각 캐릭터별 스킬 수 `K`
- 스킬 정보
  - 이름, 타입(단일 공격/회복/버프/디버프 등), 계수, 고정 피해/회복량, 쿨타임, 효과(기절/도트/방어 증가 등)
- 예시:
```plaintext
Warrior의 스킬 수를 입력하세요.
2

스킬 이름, 타입, 계수, 고정값, 쿨타임, 효과를 입력하세요.
PowerStrike,ATTACK,1.6,50,2,NONE
GuardStance,BUFF,0,0,3,DEF_UP
```

3) 시뮬레이션 설정 입력
- 두 캐릭터 간 라운드 수 (예: 1000판)
- 전투 로그 출력 여부 (요약/상세/없음)
- 무작위 시드 값 (같은 시드를 사용하면 동일 결과 재현 가능)
```plaintext
캐릭터 간 전투를 몇 판씩 시뮬레이션할지 입력하세요.
1000

전투 로그 출력 모드를 선택하세요. (NONE / SUMMARY / DETAIL)
SUMMARY

난수 시드 값을 입력하세요. (엔터 시 랜덤)
42
```

4) 분석 옵션 입력
- 밸런스 기준 정의(예: “승률이 40%~60% 사이면 밸런스 양호로 본다”)
- 분석 대상 지표 선택(승률, 평균 턴 수, 평균 남은 체력 비율 등)
```plaintext
밸런스 양호로 볼 승률 하한, 상한을 입력하세요. (예: 0.4 0.6)
0.4 0.6

분석할 지표를 선택하세요. (WIN_RATE / TURN / HP_REMAIN / ALL)
ALL
``` 
---

### ▫ 출력
1) 캐릭터 목록 출력
- 등록된 모든 캐릭터의 기본 스탯 및 스킬 요약
```plaintext
등록된 캐릭터 목록
---
Warrior  HP:1500  ATK:80  DEF:50  SPD:30  CRIT:0.10  (스킬 2개)
Mage     HP:900   ATK:120 DEF:20  SPD:40  CRIT:0.25  (스킬 2개)
Archer   HP:1000  ATK:90  DEF:30  SPD:35  CRIT:0.15  (스킬 1개)
```
2) 개별 매치업 통계 결과
- 캐릭터 A vs 캐릭터 B에 대해
  - 총 전투 수, A/B 승리 횟수 및 승률
  - 평균 전투 턴 수
  - 평균 남은 체력 비율
```plaintext
[Matchup] Warrior vs Mage
총 전투 수: 1000
Warrior 승: 630 (63.0%)
Mage 승:    370 (37.0%)
평균 전투 턴 수: 7.4
Warrior 평균 남은 HP 비율(승리 시): 0.32
Mage 평균 남은 HP 비율(승리 시):    0.18
```
3) 전체 밸런스 리포트
- 각 캐릭터별 전체 승률/패배율
- 밸런스 양호/불균형/극단적 OP 분류
- 밸런스 지수(예: 승률 분산, 편차 등)를 통한 종합 평가
```plaintext
전체 밸런스 리포트
---
Warrior: 전체 승률 58.2%  (밸런스 양호 상한 근접)
Mage   : 전체 승률 27.5%  (심각한 하향 필요)
Archer : 전체 승률 14.3%  (극단적 약체)

밸런스 평가
- Warrior는 대부분의 매치업에서 우세하나, 특정 조합에서만 크게 압도하지는 않음.
- Mage와 Archer는 다수 매치업에서 승률이 30% 이하로 떨어짐 → 스킬 계수 상향 또는 방어력/HP 조정 필요.

종합 밸런스 지수: 0.34 (0에 가까울수록 이상적, 1에 가까울수록 심각한 불균형)
```

---

## 🧾 구현할 기능 목록
### 1) 캐릭터/스킬 입력 및 검증
- [] 캐릭터 수 `N`을 입력받고, 1 이상인지 검증한다.
- [] 각 캐릭터에 대해 `이름, HP, ATK, DEF, SPD, CRIT`를 파싱한다.
  - [] HP/ATK/DEF/SPD는 양의 정수인지 검증한다.
  - [] CRIT는 0 이상 1 이하의 실수인지 검증한다.
  - [] 이름은 공백/빈 문자열을 허용하지 않는다.
  - [] 이름이 중복되면 에러를 발생시킨다.
- [] 각 캐릭터에 대해 스킬 수 `K`를 입력받고, 스킬을 정의한다.
  - [] 스킬 이름, 타입(열거형), 계수, 고정값, 쿨타임, 효과를 파싱한다.
  - [] 스킬 타입/효과는 정해진 Enum 값 중 하나인지 검증한다.
  - [] 계수/고정값/쿨타임 값의 범위를 검증한다.
- [] 입력 단계에서 오류가 나면 `[ERROR]` 메시지를 출력하고 **해당 단계부터 재입력** 받는다.
- [] 캐릭터/스킬 입력을 모두 마친 후 요약 정보를 출력한다.

### 2) 전투 규칙 및 전투 엔진
- [] **턴제 1:1 전투 규칙**을 정의한다.
  - [] SPD(속도)가 높은 캐릭터가 먼저 공격한다.
  - [] 같은 속도일 경우 우선순위를 정의한다(이름 순/랜덤 등).
  - [] 각 턴마다 공격자는 스킬 또는 기본 공격을 선택한다.
- [] 기본 공격/스킬 활용 시 데미지 계산 방식을 정의한다.
  - [] 기본 공격: `damage = max(0, ATK * 공격 계수 - DEF * 방어 계수)` 형태로 계산한다.
  - [] 치명타 확률에 따라 치명타 배율(예: 1.5배)을 적용한다.
  - [] HP는 0 미만으로 내려가지 않도록 보정한다.
- [] 스킬 시스템
  - [] 스킬 타입에 따라 처리 로직을 분리한다.
    - ATTACK: 추가 계수 또는 고정 피해량을 계산에 포함한다.
    - HEAL: HP 회복, 최대 HP를 초과하여 회복하지 않는다. 
    - BUFF/DEBUFF: 일정 턴 동안 ATK/DEF/CRIT 등의 스탯을 변경한다.
    - DOT: 매 턴 고정 피해를 주는 상태를 건다. 
    - STUN: 일정 턴 동안 상대의 행동을 막는다.
  - [] 스킬 쿨타임 및 지속 효과를 전투 상태에 반영한다.
- [] 간단한 AI 전략을 구현한다.
  - [] 공격형 AI: 가장 피해량이 큰 스킬을 우선 사용한다.
  - [] 안전형 AI: 남은 HP가 낮을 때 방어/회복 스킬을 우선 사용한다.
  - [] 랜덤형 AI: 사용 가능 스킬 중 랜덤 선택한다.
- [] 전투 종료 조건
  - [] 한쪽 HP가 0 이하가 되면 전투를 종료한다.
  - [] 특정 최대 턴 수를 넘길 경우(예: 50턴) 무승부로 처리하거나 특별 규칙을 적용한다.
- [] 전투 결과를 `BattleResult`로 반환한다.
  - [] 승자/패자
  - [] 총 턴 수
  - [] 승자의 남은 HP 비율
  - [] 사용된 스킬 로그(선택)

### 3) 시뮬레이션 실행 및 통계 집계
- [] 시뮬레이션 설정을 입력받는다.
  - [] 각 매치업당 전투 횟수(라운드 수)를 입력받고 검증한다.
  - [] 로그 출력 모드(NONE/SUMMARY/DETAIL)를 입력받는다. 
  - [] 난수 시드를 입력받고, 입력이 없으면 시스템 시간 기반 시드로 난수를 초기화한다.
- [] 등록된 모든 캐릭터들에 대해 순서쌍 매치업을 구성한다.
  - [] A vs B / B vs A를 동일 매치업으로 볼지, 방향성을 둘지 정책을 정의한다. 
  - [] 자기 자신과의 전투를 허용할지 여부를 정책으로 정한다.
- [] 각 매치업에 대해 전투를 지정된 횟수만큼 반복 실행한다. 
  - [] 매 전투마다 `BattleSimulator`를 호출하여 `BattleResult`를 얻는다. 
  - [] 승자/패자/턴 수/남은 체력을 누적 수집한다.
- [] 매치업별 통계를 계산한다. 
  - [] 캐릭터 A의 승리 횟수, 승률 
  - [] 캐릭터 B의 승리 횟수, 승률 
  - [] 평균 전투 턴 수 
  - [] 승리 시 평균 남은 HP 비율
- [] 캐릭터별 전체 통계를 계산한다. 
  - [] 전체 승/패/무승부 수 
  - [] 전체 승률 
  - [] 평균 턴 수, 평균 남은 HP 비율

### 4) 밸런스 판단 로직
- [] 사용자가 정의한 밸런스 기준 승률 범위를 입력받는다.
  - 예: 0.4 ~ 0.6 범위이면 “밸런스 양호”
- [] 각 캐릭터에 대해 전체 승률을 기준으로 밸런스 상태를 분류한다.
  - [] 범위 내 → “밸런스 양호” 
  - [] 상한 초과 → “OP/너프 필요” 
  - [] 하한 미만 → “약체/버프 필요”
- [] 전체 캐릭터의 승률 분포로부터 밸런스 지수를 계산한다.
  - [] 승률 평균과의 편차, 분산 등의 수치를 이용하여 0~1 범위로 정규화한다.
- [] 밸런스 리포트를 도메인 객체(`BalanceReport`)로 구성한다.
  - [] 캐릭터별 요약 정보
  - [] 매치업별 상세 통계
  - [] 전체 지표 및 해석 메시지

### 5) 입출력/뷰 레이어
- [] `InputView`
  - [] 캐릭터 수, 캐릭터/스킬 정의, 시뮬레이션 설정, 밸런스 기준 입력을 담당한다. 
  - [] 콘솔 입력을 읽어서 문자열로 반환하고, 파싱/검증은 도메인 또는 별도 유틸에서 처리한다.
- [] `OutputView`
  - [] 캐릭터 목록 요약 출력 
  - [] 개별 매치업 통계 출력 
  - [] 전체 밸런스 리포트 출력 
  - [] 에러 메시지 출력 (`[ERROR]` 형식)

### 6) 도메인/설계
- [] `Character`
    - [] 이름, HP, ATK, DEF, SPD, CRIT, 스킬 목록, AI 타입을 가진다. 
    - [] 생성 시 유효성 검증을 수행한다. 
    - [] 현재 전투 상태를 위한 `BattleCharacter`와 구분하여 설계한다(원본 vs 전투용 인스턴스).
- [] `Skill`
  - [] 이름, 타입, 계수, 고정값, 쿨타임, 효과, 지속 턴 수 등을 가진다. 
  - [] 타입/효과는 Enum으로 관리한다.
- [] `BattleState`
  - [] 전투 중 캐릭터별 HP, 버프/디버프, 쿨타임 상태 등을 저장한다. 
  - [] 매 턴 업데이트 로직을 가진다.
- [] `BattleResult` 
  - [] 승자/패자, 턴 수, 남은 HP 비율, 로그 등 결과 정보를 가진다.
- [] `SimulationConfig` 
  - [] 라운드 수, 로그 모드, 난수 시드, 밸런스 기준 등을 포함한다.
- [] `BalanceReport`
  - []전체 통계와 캐릭터/매치업별 통계를 포함하는 리포트 객체이다.

### 7) 리팩터링/제약 준수
- [] 들여쓰기(depth) ≤ 2를 유지한다.
- [] 한 메서드는 한 가지 일만 하며 15라인 이하를 지향한다.
- [] `else`, `switch/case`, 3항 연산자 사용을 최소화하고 조기 반환을 활용한다.
- [] 숫자/문자열 하드 코딩을 피하고, 의미 있는 `static final` 상수로 관리한다.
- [] 도메인 규칙은 가능한 한 도메인 객체 내부에 위치시킨다.
- [] 전투 로직과 입출력을 철저히 분리하여 테스트 가능성을 높인다.

---

## ⚠️ 예외 처리 규칙

| 상황                     | 처리 방식                                              |
| ---------------------- | -------------------------------------------------- |
| 캐릭터 수가 1 미만            | `[ERROR] 캐릭터 수는 1 이상이어야 한다.` 출력 후 캐릭터 수부터 재입력      |
| 캐릭터 스탯 파싱 실패           | `[ERROR] 캐릭터 입력 형식이 올바르지 않다.` 출력 후 해당 캐릭터부터 재입력    |
| HP/ATK/DEF/SPD가 0 이하   | `[ERROR] 스탯은 1 이상이어야 한다.` 출력 후 해당 캐릭터부터 재입력        |
| CRIT가 0~1 범위를 벗어남      | `[ERROR] 치명타 확률은 0 이상 1 이하여야 한다.` 출력 후 재입력         |
| 캐릭터 이름 중복              | `[ERROR] 캐릭터 이름은 중복될 수 없다.` 출력 후 해당 캐릭터부터 재입력      |
| 스킬 타입/효과가 정의되지 않은 값    | `[ERROR] 존재하지 않는 스킬 타입(또는 효과)이다.` 출력 후 해당 스킬부터 재입력 |
| 라운드 수 ≤ 0              | `[ERROR] 시뮬레이션 횟수는 1 이상이어야 한다.` 출력 후 재입력           |
| 밸런스 기준 범위가 0~1을 벗어남    | `[ERROR] 승률 기준은 0 이상 1 이하여야 한다.` 출력 후 재입력          |
| 밸런스 하한 > 상한            | `[ERROR] 밸런스 하한은 상한 이하여야 한다.` 출력 후 재입력             |
| 입력 파싱 실패(비숫자, 형식 오류 등) | `[ERROR] 입력 형식이 올바르지 않다.` 출력 후 해당 단계 재입력           |

> 예외는 `IllegalArgumentException`, `IllegalStateException` 등 명확한 유형으로 처리한다.
> 에러 메시지는 반드시 `"[ERROR]"`로 시작한다.

---
## 🧪 테스트 케이스 (JUnit5 + AssertJ)
| 구분                 | 입력/상황                     | 기대 결과                |
| ------------------ | ------------------------- | -------------------- |
| 캐릭터 – 정상 생성        | Warrior,1500,80,50,30,0.1 | Character 인스턴스 생성 성공 |
| 캐릭터 – 이름 공백        | `""`                      | 예외 발생                |
| 캐릭터 – 스탯 0 이하      | HP=0                      | 예외 발생                |
| 캐릭터 – CRIT 범위 오류   | 1.5                       | 예외 발생                |
| 캐릭터 – 이름 중복        | Warrior 두 번               | 예외 발생                |
| 스킬 – 타입/효과 Enum 매핑 | 올바른 문자열                   | 해당 Enum으로 매핑         |
| 스킬 – 잘못된 타입        | UNKNOWN                   | 예외 발생                |
| 전투 – 기본 공격 데미지 계산  | ATK=100, DEF=50           | 기대 값만큼 데미지           |
| 전투 – 치명타 적용        | CRIT=1.0                  | 항상 치명타 배율 적용         |
| 전투 – HP 0 미만 방지    | 큰 피해                      | HP는 0으로 고정           |
| 전투 – 선공 결정         | SPD가 더 큰 캐릭터              | 먼저 공격                |
| 전투 – 턴 수 카운트       | 간단한 전투                    | 기대 턴 수로 종료           |
| 전투 – 최대 턴 초과       | 50턴 넘게 지속                 | 무승부 또는 특수 처리         |
| AI – 공격형 전략        | 여러 스킬 보유                  | 가장 피해 기대값 큰 스킬 선택    |
| 시뮬레이션 – 승률 집계      | Warrior vs Mage 100판      | 승/패 수, 승률이 합리적인 범위   |
| 밸런스 – 기준 내 승률      | 승률 0.45                   | “밸런스 양호” 분류          |
| 밸런스 – OP 판단        | 승률 0.8                    | “OP/너프 필요” 분류        |
| 밸런스 – 약체 판단        | 승률 0.2                    | “약체/버프 필요” 분류        |
| 밸런스 지수 계산          | 승률 분포 다양                  | 0~1 사이 값 반환          |
| 리포트 – 캐릭터별 요약      | 여러 캐릭터                    | 이름, 승률, 상태가 올바르게 포함  |

> 전투/시뮬레이션 로직은 난수에 의존하므로, 테스트에서는 **고정 시드** 또는 **난수 전략 주입**을 통해 결정성을 확보한다.

---
## 🧱 프로젝트 구조

### 1) 콘솔 기반 순수 Java 버전
```plaintext
src
└─ main/
   └─ java/
      └─ balance/
         ├─ Application.java                # 진입점 (메인 루프)
         ├─ controller/
         │   └─ SimulationController.java   # 입력 → 시뮬레이션 → 리포트 출력 흐름 제어
         ├─ application/
         │   ├─ CharacterService.java       # 캐릭터/스킬 등록 및 조회
         │   ├─ SimulationService.java      # 시뮬레이션 실행 및 집계
         │   └─ BalanceAnalysisService.java # 밸런스 판단 및 리포트 생성
         ├─ domain/
         │   ├─ Character.java
         │   ├─ Skill.java
         │   ├─ SkillType.java              # Enum
         │   ├─ SkillEffect.java            # Enum
         │   ├─ BattleCharacter.java        # 전투용 상태 객체
         │   ├─ BattleState.java
         │   ├─ BattleResult.java
         │   ├─ SimulationConfig.java
         │   └─ BalanceReport.java
         ├─ battle/
         │   ├─ BattleSimulator.java        # 1:1 전투 엔진
         │   ├─ TurnExecutor.java           # 턴 진행 로직
         │   └─ AiStrategy.java             # AI 전략 인터페이스 및 구현체
         ├─ view/
         │   ├─ InputView.java
         │   └─ OutputView.java
         └─ support/
             ├─ RandomProvider.java         # 난수 전략 분리
             └─ Constants.java              # 상수 및 메시지
```

### 2) (선택:추후에 확장가능) Spring Boot 기반 REST API 확장
```plaintext
└─ main/java/balance/api/
   ├─ BalanceApplication.java               # Spring Boot 진입점
   ├─ controller/
   │   ├─ CharacterApiController.java       # 캐릭터/스킬 CRUD
   │   └─ SimulationApiController.java      # 시뮬레이션 요청/리포트 조회
   └─ dto/
       ├─ CharacterRequest.java
       ├─ CharacterResponse.java
       ├─ SimulationRequest.java
       └─ BalanceReportResponse.java
```
---
- 콘솔 버전을 먼저 완성하고, 필요 시 동일 도메인을 활용해 API 레이어를 얹는 방식으로 확장한다.
- 도메인/서비스 레이어는 프레임워크에 의존하지 않도록 설계한다.
---

## 🧾 커밋 컨벤션 (AngularJS Style)
| 타입              | 설명       | 예시                                                |
| --------------- | -------- | ------------------------------------------------- |
| `docs:`         | 문서       | `docs: README에 프로젝트 개요 및 기능 목록 추가`                |
| `feat(domain):` | 도메인 모델   | `feat(domain): Character/Skill 도메인 및 유효성 검증 추가`   |
| `feat(domain):` | 전투 규칙    | `feat(domain): BattleResult 및 BattleState 구현`     |
| `feat(battle):` | 전투 엔진    | `feat(battle): BattleSimulator 1:1 전투 로직 구현`      |
| `feat(app):`    | 시뮬레이션    | `feat(app): SimulationService로 매치업별 시뮬레이션 실행`     |
| `feat(app):`    | 밸런스 분석   | `feat(app): BalanceAnalysisService로 승률 기반 밸런스 평가` |
| `feat(view):`   | 콘솔 I/O   | `feat(view): InputView/OutputView 콘솔 입출력 구현`      |
| `feat(api):`    | REST API | `feat(api): 캐릭터 등록/조회 API 기본 구현`                  |
| `test:`         | 테스트      | `test: Character/Skill/전투 로직 단위 테스트 추가`           |
| `refactor:`     | 리팩터링     | `refactor: 전투 로직 분리 및 indent 2단계로 정리`             |
| `fix:`          | 버그 수정    | `fix: 치명타 배율 계산 오류 수정`                            |

---

## 💡 개발 진행 방식
1) README 기능 목록 작성
- 구현할 기능을 모두 나열하고, 필수/심화 기능을 구분한다.
2) 도메인부터 TDD로 시작
- Character, Skill, BattleResult, SimulationConfig부터 테스트 → 구현 → 리팩터링 순으로 진행한다.
3) 전투 엔진 구현
- 매우 작은 규칙(기본 공격만)으로 시작해서, 치명타 → 스킬 → 상태이상 순으로 점진적으로 확장한다.
4) 시뮬레이션 + 통계 집계 구현
- 두 캐릭터만 대상으로 테스트 → 여러 캐릭터 확장 → 전체 매치업 확장 순으로 진행한다.
5) 밸런스 분석 로직 구현
- 승률 기준 정의 → 상태 분류 → 지수 계산 → 리포트 객체로 묶는다.
6) 콘솔 입출력 연결
- 도메인/서비스가 안정되면 InputView/OutputView를 붙여 실제 흐름을 완성한다.
7) 리팩터링 및 문서화
- 메서드 길이/indent/하드 코딩/네이밍 등을 점검하며 구조를 다듬고, “어떤 기준으로 밸런스를 정의했는지, 어떤 시행착오가 있었는지”를 문서로 남긴다.
---
## ⚙️ 개발 환경
- 언어: Java 21
- 빌드 도구: Gradle
- 테스트: JUnit 5 + AssertJ
- IDE: IntelliJ IDEA
---
## 📚 참고 자료
- JUnit 5 User Guide
- AssertJ User Guide / Exception Assertions
- Guide to JUnit 5 Parameterized Tests
- 우아한테크코스 Java Style Guide
- Angular Commit Message Conventions